<div class="email-notifications form-wrapper">
  <%= form_for @company do |f| %>
    <p class="form-label">Site Owner Email</p>
    <%= f.text_field :email %>
    <p class="form-label">Positive Message Email</p>
    <%= f.text_field :email %>
    <p class="form-label">Negative Message Email</p>
    <%= f.text_field :email %>
    <%= f.submit "Save" %>
  <% end %>
</div>
